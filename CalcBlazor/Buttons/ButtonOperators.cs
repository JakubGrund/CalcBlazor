namespace CalcBlazor.Buttons
{
    public class ButtonOperators
    {
        /*private void button39_Click(object sender, EventArgs e)
        {
            switch (abbb)
            {
                case "+"
                textBox.Text = (firstdigit + double.Parse(textBox.Text)).ToString();
                    break;
                case "-"
                textBox.Text = (firstdigit - double.Parse(textBox.Text)).ToString();
                    break;
                case "*"
                textBox.Text = (firstdigit * double.Parse(textBox.Text)).ToString();
                    break;
                case "/"
                textBox.Text = (firstdigit / double.Parse(textBox.Text)).ToString();
                    break;
            }
        }*/
    }
}
